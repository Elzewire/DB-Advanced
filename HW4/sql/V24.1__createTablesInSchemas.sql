CREATE TABLE foreign1.table (
    id integer,
    content varchar
);

CREATE TABLE foreign2.table (
    id integer,
    content varchar
);

CREATE TABLE foreign3.table (
    id integer,
    content varchar
);